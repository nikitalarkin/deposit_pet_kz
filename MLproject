name: kz-credits

docker_env:
  image: petbanks-app

entry_points:
  train_kz:
    parameters:
      db_url: {type: string, default: postgresql+psycopg2://postgres:qwerty123@db:5432}
      db_name: {type: string, default: credits_total_kz}
      mlflow_uri: {type: string, default: http://mlflow:5000}
      experiment: {type: string, default: credits_total_kz}
    command: >
      python src/train.py
